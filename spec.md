# ASK Electron版 仕様書（spec.md）

## 1. 概要

ASK Electron版は、プログラミングスクールにおいて生徒と先生・メンターをつなぐ、コード中心の質問チャットPCアプリである。

GitHubをコード・履歴・差分の基盤として利用し、ASKは質問・診断・伴走のUIを提供する。MVPでは宿題・課題配布・自動採点は扱わず、チャット、コード差分、環境診断、AI補助、先生によるパッチ提案、生徒承認によるローカル適用に集中する。

---

## 2. 背景・目的

### 2.1 背景

プログラミング初学者は、エラー発生時に以下を正確に伝えることが難しい。

- 何をしようとしていたか
- 期待した結果と実際の結果
- エラー文
- 関連するコード
- 直近の変更差分
- OS、言語、ランタイム、依存関係、エディターなどの環境情報

先生・メンター側も、情報不足のまま原因調査を始めるため、質問対応に時間がかかりやすい。

### 2.2 目的

初学者の「動きません」を、先生・AIがすぐ調査できる質問に変換し、メンターが生徒の開発に伴走しやすい環境を提供する。

### 2.3 プロダクトの立ち位置

GitHub Classroomが「課題管理・提出・採点」を担うツールであるのに対し、ASK Electron版は「ローカル開発環境で詰まった瞬間の質問・診断・修正提案」を担う。

---

## 3. 想定ユーザー

### 3.1 生徒

- 中学1年生以上のプログラミング学習者
- GitHubやGitに不慣れな初学者を含む
- ローカルPC上で開発する
- エラー発生時に先生またはAIへ質問する

### 3.2 先生・メンター

- 生徒からの質問に回答する
- コード差分、環境情報、ログを確認して原因を調査する
- チャット上で修正パッチを提案する
- 生徒の質問対応状況を管理する

### 3.3 スクール管理者

- クラスを作成・管理する
- 先生・メンター・生徒を管理する
- クラス単位で質問対応状況を見る

MVPでは管理者機能は最小限とし、先生がクラス作成・生徒招待できることを重視する。

---

## 4. スコープ

### 4.1 MVPに含めるもの

- Electron製PCアプリ
- Supabase Auth / Postgres / Realtime / Functions
- GitHub連携必須のプロジェクト作成
- Git / GitHub / SSH接続オンボーディング
- クラス作成
- 生徒招待
- プロジェクト作成
- スレッド作成
- チャット
- コードスニペット表示
- シンタックスハイライト
- Git差分表示
- 生徒PCの環境情報収集
- 送信前プレビュー
- `.gitignore` 自動生成・補助
- 秘密情報チェック
- AI質問文生成
- AIエラー要約
- AI原因候補提示
- AIパッチ案生成
- 先生によるパッチ提案
- 生徒承認によるローカルパッチ適用
- 先生向け質問キュー

### 4.2 MVPに含めないもの

- 宿題機能
- 課題配布
- テンプレートリポジトリ配布
- 自動採点
- 成績管理
- LMS連携
- GitHub Classroom完全互換
- 先生による生徒PCの直接書き換え
- 先生によるリモート操作
- 自動Pull Request作成
- 複雑なブランチ運用
- 画面共有
- 音声通話
- 決済
- モバイルアプリ

### 4.3 将来拡張候補

- 宿題・課題機能
- GitHub Classroom連携
- Pull Request作成
- GitHub Actionsログ連携
- 類似質問検索
- ナレッジベース化
- 回答テンプレート
- スクール全体の分析ダッシュボード
- VS Code拡張

---

## 5. 確定している重要方針

### 5.1 GitHub必須

GitHub連携はMVPで必須とする。GitHub連携が完了していない場合、プロジェクト作成はできない。

### 5.2 ASKの立ち位置

GitHubをコード・履歴・差分の基盤として使い、ASKは質問・診断・伴走のUIを提供する。

### 5.3 パッチ適用方針

先生・AIはパッチを提案できるが、生徒PCへ直接適用できない。必ず生徒が差分を確認し、自分の操作でローカルに適用する。

「先生が直接書き換える」機能は実装しない。

### 5.4 ブランチ運用

MVPでは複雑なブランチ運用を要求しない。生徒はmainブランチ1本で開発してよい。パッチ提案はPRではなく、ASKのチャットUI上に表示し、生徒が1クリックでローカル適用できるようにする。

### 5.5 宿題機能

MVPには含めない。MVPはチャットと質問対応に集中する。

---

## 6. ユースケース

### 6.1 生徒が初回セットアップする

1. 生徒がASKアプリを起動する
2. ASKアカウントを作成またはログインする
3. クラス招待URLまたは招待コードからクラスに参加する
4. GitHub連携を行う
5. Git CLIの有無を確認する
6. GitHub認証を行う
7. SSH鍵の有無を確認する
8. SSH接続をテストする
9. プロジェクトフォルダを選択する
10. `.git` の有無を確認する
11. `.gitignore` の有無を確認し、必要なら生成する
12. GitHubリポジトリと紐付ける
13. プロジェクト作成を完了する

### 6.2 生徒が質問する

1. 生徒がプロジェクトを選ぶ
2. 「質問を作成」を押す
3. エラー文、実行コマンド、状況説明を入力する
4. 関連ファイルを選択する
5. ASKがGit差分、環境情報、依存関係情報を収集する
6. 秘密情報チェックを行う
7. 送信前プレビューを表示する
8. AIが質問文を整形する
9. 生徒が確認して送信する
10. 先生の質問キューに追加される

### 6.3 AIに質問する

1. 生徒が「AIに聞く」を選ぶ
2. ASKがエラー文、関連コード、環境情報、Git差分をAIへ渡す
3. AIがエラー要約、原因候補、修正案を返す
4. 修正案がコード変更を含む場合はパッチ案として表示する
5. 生徒が差分を確認する
6. 生徒が必要に応じてパッチをローカルに適用する
7. 解決しない場合は、AIとのやり取りごと先生にエスカレーションする

### 6.4 先生が質問に回答する

1. 先生が質問キューを見る
2. 未対応または対応中の質問を開く
3. 質問文、エラー文、関連ファイル、Git差分、環境情報を確認する
4. チャットで回答する
5. 必要に応じてコード修正パッチを作成する
6. 生徒にパッチを送信する
7. 生徒が差分を確認して適用する
8. 生徒が動作確認する
9. 解決済みにする

### 6.5 生徒がパッチを適用する

1. 生徒が先生またはAIから届いたパッチ提案を見る
2. 変更対象ファイル、変更内容、説明を確認する
3. 適用前バックアップ作成を確認する
4. 「適用」を押す
5. ASKがローカルファイルにパッチを適用する
6. 適用結果を表示する
7. 必要に応じて実行コマンドを案内する
8. 生徒が動作確認する
9. 問題があれば「元に戻す」を実行できる

---

## 7. 機能要件

### 7.1 認証・アカウント

- 概要: 生徒・先生がASKにログインする
- 利用者: 生徒、先生、管理者
- 入力:
  - メールアドレス
  - パスワード
  - 外部認証情報
- 処理:
  - Supabase Authで認証する
  - ユーザーのロールを取得する
- 出力:
  - ログイン済みセッション
- 例外:
  - 認証失敗
  - 招待されていないクラスへの参加
- 権限:
  - 生徒: 自分の所属クラス・プロジェクトのみ
  - 先生: 担当クラス・担当生徒
  - 管理者: 所属組織全体

### 7.2 GitHubオンボーディング

- 概要: 生徒がGitHubを利用できる状態まで案内する
- 利用者: 生徒
- 入力:
  - GitHubアカウント
  - GitHub認証情報
  - ローカルPC環境
- 処理:
  - GitHubアカウントの有無を確認する
  - Gitのインストール状況を確認する
  - GitHub CLIの有無を確認する
  - GitHub認証フローを案内する
  - SSH鍵の有無を確認する
  - SSH接続をテストする
- 出力:
  - GitHub連携済み状態
  - SSH接続成功状態
- 例外:
  - Git未インストール
  - GitHub未ログイン
  - SSH鍵未設定
  - SSH接続失敗
- 備考:
  - PAT手入力は原則避ける
  - 中学1年生以上でも理解しやすいUIを優先する

### 7.3 GitHub認証方式

- 概要: GitHubとの接続方式を決定・実行する
- 利用者: 生徒、先生
- 処理:
  - 優先方式として `gh auth login` を検討する
  - UI上で手順を案内する
  - 代替としてGitHub Device Flowを検討する
  - PAT手入力は最終手段とする
- 出力:
  - GitHub操作可能状態
- 例外:
  - `gh` コマンド未インストール
  - 認証失敗
  - 権限不足
- 備考:
  - MVP実装前に、`gh auth login` とDevice FlowのUX比較を行う

### 7.4 SSH接続確認

- 概要: GitHubへのSSH接続を確認する
- 利用者: 生徒
- 処理:
  - 既存SSH鍵を検出する
  - SSH鍵がない場合は作成を補助する
  - 公開鍵をGitHubへ登録する方法を案内する
  - `ssh -T git@github.com` 相当の接続テストを実行する
- 出力:
  - SSH接続成功または失敗状態
- 例外:
  - SSH鍵がない
  - GitHub側に公開鍵が未登録
  - ネットワークエラー

### 7.5 クラス作成

- 概要: 先生がクラスを作成する
- 利用者: 先生、管理者
- 入力:
  - クラス名
  - 説明
  - 期間 任意
  - メンター設定 任意
- 処理:
  - クラスを作成する
  - 招待URLまたは招待コードを発行する
- 出力:
  - クラス詳細画面
  - 招待情報
- 例外:
  - 同名クラス
  - 権限不足
- 備考:
  - MVPでは教材テンプレート配布は行わない

### 7.6 生徒招待

- 概要: 先生が生徒をクラスへ招待する
- 利用者: 先生
- 入力:
  - 招待URL
  - 招待コード
- 処理:
  - 生徒が招待を受諾する
  - クラスメンバーとして登録する
- 出力:
  - 生徒一覧に追加
- 例外:
  - 招待期限切れ
  - 既に参加済み

### 7.7 プロジェクト作成

- 概要: 生徒がGitHub連携済みのローカルプロジェクトをASKに登録する
- 利用者: 生徒
- 入力:
  - ローカルフォルダ
  - GitHubリポジトリ
  - プロジェクト名
- 処理:
  - GitHub連携済みか確認する
  - `.git` の有無を確認する
  - remote originを確認する
  - `.gitignore` の有無を確認する
  - 必要に応じて `.gitignore` を生成する
  - GitHubリポジトリと紐付ける
- 出力:
  - プロジェクト詳細
- 例外:
  - GitHub未連携
  - `.git` がない
  - remoteがない
  - GitHubリポジトリと不一致

### 7.8 `.gitignore` 自動生成・補助

- 概要: 秘密情報や不要ファイルをGit管理・送信対象から除外する
- 利用者: 生徒
- 処理:
  - プロジェクト種別を推定する
  - 推奨 `.gitignore` を提示する
  - 既存 `.gitignore` に追記候補を提示する
  - 生徒の確認後に作成・更新する
- 出力:
  - `.gitignore` 作成・更新結果
- 備考:
  - `.env`, `node_modules`, `.DS_Store`, build成果物などを候補に含める

### 7.9 スレッド作成

- 概要: 生徒が質問スレッドを作成する
- 利用者: 生徒
- 入力:
  - タイトル
  - 状況説明
  - エラー文
  - 実行コマンド
  - 関連ファイル
  - タグ 任意
- 処理:
  - Git状態を取得する
  - 環境情報を取得する
  - 送信前プレビューを生成する
  - AI質問文生成を実行する 任意
  - スレッドを作成する
- 出力:
  - 質問スレッド
- 例外:
  - プロジェクト未登録
  - Git状態取得失敗
  - 秘密情報検出

### 7.10 チャット

- 概要: 生徒と先生がスレッド内でやり取りする
- 利用者: 生徒、先生
- 入力:
  - テキスト
  - コードスニペット
  - 添付情報
  - パッチ提案
- 処理:
  - メッセージを保存する
  - リアルタイム配信する
  - シンタックスハイライトを適用する
- 出力:
  - チャットメッセージ

### 7.11 環境情報収集

- 概要: 生徒PCの開発環境情報を収集する
- 利用者: 生徒
- 収集対象:
  - OS名・バージョン
  - CPUアーキテクチャ
  - Gitバージョン
  - Node / npm / pnpm / yarn
  - Python / pip
  - その他カリキュラムで必要なランタイム
  - エディター情報
  - 依存関係ファイル
  - Gitブランチ
  - HEAD commit
  - 未コミット差分
- 備考:
  - 収集対象はホワイトリスト方式
  - 送信前プレビュー必須

### 7.12 秘密情報チェック

- 概要: `.env`、APIキー、秘密鍵、トークンなどの送信を防ぐ
- 利用者: 生徒
- 処理:
  - ファイル名チェック
  - パターンマッチング
  - 高リスク情報を検出
  - 送信をブロックまたは警告
- 備考:
  - `.env`、SSH秘密鍵、GitHubトークンは原則送信不可

### 7.13 AI質問文生成

- 概要: 生徒の入力を先生が読みやすい質問文に整える
- 利用者: 生徒
- 入力:
  - 状況説明
  - エラー文
  - 実行コマンド
  - 関連コード
  - 環境情報
- 出力:
  - 生成済み質問文
- 備考:
  - 生徒が送信前に必ず確認・編集できる

### 7.14 AIエラー要約・原因候補提示

- 概要: エラー内容をAIが要約し、原因候補を提示する
- 利用者: 生徒、先生
- 出力:
  - エラー要約
  - 原因候補
  - 次に確認すべきこと
- 備考:
  - AI回答は確定回答ではなく補助情報として扱う

### 7.15 AIパッチ案生成

- 概要: AIが修正パッチ案を生成する
- 利用者: 生徒、先生
- 出力:
  - パッチ案
  - 変更理由
- 備考:
  - AIパッチも生徒承認なしに適用してはならない

### 7.16 先生によるパッチ提案

- 概要: 先生がチャット上で修正パッチを提案する
- 利用者: 先生
- 入力:
  - 対象ファイル
  - 修正内容
  - 変更理由
- 出力:
  - パッチ提案メッセージ
- 備考:
  - 先生は生徒PCへ直接適用できない

### 7.17 生徒承認によるパッチ適用

- 概要: 生徒がパッチ内容を確認し、ローカルに適用する
- 利用者: 生徒
- 処理:
  - 対象ファイルの現在状態を確認する
  - 適用前バックアップを作成する
  - パッチを適用する
  - 適用結果を表示する
- 例外:
  - コンフリクト
  - ファイルが変更済み
  - 書き込み権限なし
- 備考:
  - 1クリック適用を目指す
  - 元に戻す機能を提供する

### 7.18 先生向け質問キュー

- 概要: 先生が未対応質問を管理する
- 利用者: 先生
- 表示:
  - 未対応
  - 対応中
  - 生徒確認待ち
  - パッチ提案済み
  - 解決済み
  - 再オープン
- 備考:
  - MVPでは高度な分析は不要

---

## 8. 画面仕様

### 8.1 ログイン画面

- 目的: ASKにログインする
- 表示項目:
  - メールアドレス
  - パスワード
  - ログインボタン
  - アカウント作成リンク
- 遷移:
  - ログイン成功後、ロールに応じたホームへ

### 8.2 生徒オンボーディング画面

- 目的: GitHub / Git / SSH / プロジェクト接続を完了する
- 表示項目:
  - GitHubアカウント確認
  - Gitインストール確認
  - GitHub認証確認
  - SSH接続確認
  - プロジェクトフォルダ選択
  - GitHubリポジトリ紐付け
- 操作:
  - 接続確認
  - 再試行
  - 手順を見る
  - 次へ
- エラー表示:
  - Git未インストール
  - GitHub未認証
  - SSH接続失敗
  - リポジトリ不一致

### 8.3 先生ホーム

- 目的: 質問対応状況を一覧する
- 表示項目:
  - 担当クラス
  - 未対応質問数
  - 対応中質問
  - 生徒一覧
  - GitHub/SSH初期設定状況
- 操作:
  - クラスを開く
  - 質問を開く
  - 生徒を確認する

### 8.4 クラス詳細画面

- 目的: クラス内の生徒と質問を管理する
- 表示項目:
  - クラス名
  - 生徒一覧
  - メンター一覧
  - 招待リンク
  - 質問一覧
- 操作:
  - 生徒招待
  - 質問を開く
  - 生徒詳細を開く

### 8.5 プロジェクト作成画面

- 目的: ローカルプロジェクトをASKに登録する
- 表示項目:
  - フォルダ選択
  - Git状態
  - GitHubリポジトリ
  - `.gitignore` 状態
- 操作:
  - フォルダ選択
  - GitHubリポジトリ紐付け
  - `.gitignore` 生成
  - 作成

### 8.6 スレッド作成画面

- 目的: 生徒が質問を作成する
- 表示項目:
  - タイトル
  - 状況説明
  - エラー文
  - 実行コマンド
  - 関連ファイル
  - Git差分
  - 環境情報
  - AI生成質問文
- 操作:
  - AIで質問文生成
  - 関連ファイル選択
  - 送信前プレビュー
  - 送信

### 8.7 スレッド詳細・チャット画面

- 目的: 質問に対して生徒・先生・AIがやり取りする
- 表示項目:
  - 質問本文
  - チャット
  - コードブロック
  - 差分
  - 環境情報
  - パッチ提案
  - ステータス
- 操作:
  - メッセージ送信
  - AIに聞く
  - パッチ確認
  - パッチ適用
  - 解決済みにする

### 8.8 パッチ確認画面

- 目的: 生徒が修正内容を理解して適用する
- 表示項目:
  - 変更対象ファイル
  - 変更理由
  - diff
  - 適用前バックアップ説明
- 操作:
  - 適用
  - キャンセル
  - 元に戻す

---

## 9. データ仕様

### 9.1 users

| 項目 | 型 | 必須 | 説明 |
|---|---|---:|---|
| id | uuid | 必須 | ユーザーID |
| email | text | 必須 | メールアドレス |
| display_name | text | 必須 | 表示名 |
| role | enum | 必須 | student / teacher / admin |
| github_username | text | 任意 | GitHubユーザー名 |
| created_at | timestamptz | 必須 | 作成日時 |
| updated_at | timestamptz | 必須 | 更新日時 |

### 9.2 classes

| 項目 | 型 | 必須 | 説明 |
|---|---|---:|---|
| id | uuid | 必須 | クラスID |
| organization_id | uuid | 必須 | 組織ID |
| name | text | 必須 | クラス名 |
| description | text | 任意 | 説明 |
| created_by | uuid | 必須 | 作成者 |
| created_at | timestamptz | 必須 | 作成日時 |

### 9.3 class_members

| 項目 | 型 | 必須 | 説明 |
|---|---|---:|---|
| id | uuid | 必須 | メンバーID |
| class_id | uuid | 必須 | クラスID |
| user_id | uuid | 必須 | ユーザーID |
| role | enum | 必須 | student / teacher / mentor |
| joined_at | timestamptz | 必須 | 参加日時 |

### 9.4 projects

| 項目 | 型 | 必須 | 説明 |
|---|---|---:|---|
| id | uuid | 必須 | プロジェクトID |
| owner_user_id | uuid | 必須 | 生徒ID |
| class_id | uuid | 必須 | クラスID |
| name | text | 必須 | プロジェクト名 |
| local_path_hash | text | 任意 | ローカルパスのハッシュ |
| github_repo_url | text | 必須 | GitHubリポジトリURL |
| default_branch | text | 任意 | 既定ブランチ |
| created_at | timestamptz | 必須 | 作成日時 |

### 9.5 github_connections

| 項目 | 型 | 必須 | 説明 |
|---|---|---:|---|
| id | uuid | 必須 | 接続ID |
| user_id | uuid | 必須 | ユーザーID |
| github_username | text | 必須 | GitHubユーザー名 |
| auth_method | enum | 必須 | gh_cli / device_flow / oauth / pat |
| ssh_status | enum | 必須 | unknown / ok / failed |
| last_checked_at | timestamptz | 任意 | 最終確認日時 |

### 9.6 threads

| 項目 | 型 | 必須 | 説明 |
|---|---|---:|---|
| id | uuid | 必須 | スレッドID |
| project_id | uuid | 必須 | プロジェクトID |
| created_by | uuid | 必須 | 作成者 |
| title | text | 必須 | タイトル |
| status | enum | 必須 | open / in_progress / waiting_student / patch_proposed / resolved / reopened |
| priority | enum | 任意 | low / normal / high |
| ai_used | boolean | 必須 | AI利用有無 |
| created_at | timestamptz | 必須 | 作成日時 |
| updated_at | timestamptz | 必須 | 更新日時 |

### 9.7 messages

| 項目 | 型 | 必須 | 説明 |
|---|---|---:|---|
| id | uuid | 必須 | メッセージID |
| thread_id | uuid | 必須 | スレッドID |
| sender_user_id | uuid | 任意 | 送信者ID |
| sender_type | enum | 必須 | student / teacher / ai / system |
| body | text | 必須 | 本文 |
| message_type | enum | 必須 | text / code / patch / environment / ai_summary |
| reply_to_message_id | uuid | 任意 | 返信先 |
| created_at | timestamptz | 必須 | 作成日時 |

### 9.8 environment_snapshots

| 項目 | 型 | 必須 | 説明 |
|---|---|---:|---|
| id | uuid | 必須 | 環境スナップショットID |
| thread_id | uuid | 必須 | スレッドID |
| project_id | uuid | 必須 | プロジェクトID |
| os_name | text | 任意 | OS名 |
| os_version | text | 任意 | OSバージョン |
| arch | text | 任意 | CPUアーキテクチャ |
| git_version | text | 任意 | Gitバージョン |
| editor_name | text | 任意 | エディター名 |
| editor_version | text | 任意 | エディターバージョン |
| runtimes | jsonb | 任意 | Node/Python等のバージョン |
| package_managers | jsonb | 任意 | npm/pnpm/yarn等 |
| dependencies_summary | jsonb | 任意 | 依存関係概要 |
| created_at | timestamptz | 必須 | 作成日時 |

### 9.9 patch_proposals

| 項目 | 型 | 必須 | 説明 |
|---|---|---:|---|
| id | uuid | 必須 | パッチID |
| thread_id | uuid | 必須 | スレッドID |
| message_id | uuid | 必須 | メッセージID |
| created_by | uuid | 任意 | 作成者 |
| created_by_type | enum | 必須 | teacher / ai |
| target_file_path | text | 必須 | 対象ファイル |
| base_commit_sha | text | 任意 | 前提コミット |
| patch_text | text | 必須 | unified diff |
| explanation | text | 任意 | 変更理由 |
| status | enum | 必須 | proposed / applied / failed / reverted / dismissed |
| created_at | timestamptz | 必須 | 作成日時 |

---

## 10. 権限仕様

| 操作 | 管理者 | 先生 | 生徒 | AI |
|---|---:|---:|---:|---:|
| クラス作成 | 可 | 可 | 不可 | 不可 |
| 生徒招待 | 可 | 可 | 不可 | 不可 |
| クラス閲覧 | 可 | 担当のみ | 所属のみ | 不可 |
| プロジェクト作成 | 不可 | 不可 | 可 | 不可 |
| プロジェクト閲覧 | 可 | 担当クラス内 | 自分のみ | 必要範囲のみ |
| スレッド作成 | 不可 | 任意 | 可 | 不可 |
| スレッド閲覧 | 可 | 担当クラス内 | 自分のスレッド | 必要範囲のみ |
| メッセージ投稿 | 可 | 担当スレッド | 自分のスレッド | システム経由のみ |
| パッチ提案 | 不可 | 可 | 不可 | 可 |
| パッチ適用 | 不可 | 不可 | 自分のローカルのみ | 不可 |
| 生徒PC直接書き換え | 不可 | 不可 | 本人操作のみ | 不可 |

---

## 11. 非機能要件

### 11.1 対応デバイス

- PCアプリを対象とする
- Electronを利用する
- MVPではmacOS + Windowsを優先候補とする
- Linux対応は未確定

### 11.2 性能

- チャット送信は通常1秒以内に反映されることを目標とする
- AI応答はストリーミング表示を検討する
- 環境情報収集は通常10秒以内を目標とする
- 大きなリポジトリでもUIが固まらないこと

### 11.3 セキュリティ

- Electronでは `contextIsolation: true`
- `nodeIntegration: false`
- preload経由で必要最小限のAPIのみ公開
- IPCで実行可能なコマンドはホワイトリスト制
- Supabase RLS必須
- GitHubトークンを平文保存しない
- 秘密情報の送信防止を実装する

### 11.4 アクセシビリティ

- 中学1年生以上が使える文言にする
- エラー文は専門用語だけで表示しない
- 次に何をすればよいかを明示する
- コード差分は色だけに依存しない

---

## 12. 外部連携

### 12.1 GitHub

- 用途:
  - リポジトリ参照
  - コミット参照
  - 差分参照
  - GitHubアカウント連携
  - SSH接続確認
- MVP方針:
  - GitHub連携必須
  - PAT手入力は避ける
  - `gh auth login` またはDevice Flowを検討
  - SSH接続までオンボーディングする
- MVPでしないこと:
  - Pull Request自動作成
  - 複雑なブランチ作成
  - GitHub Classroom連携

### 12.2 Supabase

- 用途:
  - Auth
  - Postgres
  - Realtime
  - Storage 任意
  - Functions
- 備考:
  - チャット、スレッド、クラス、環境情報、パッチメタデータを保存する
  - コード全文の大量保存は避ける

### 12.3 AI Provider

- 用途:
  - 質問文生成
  - エラー要約
  - 原因候補提示
  - パッチ案生成
- 備考:
  - AIに送信する情報は送信前プレビューと秘密情報チェックを通す
  - AI出力を保存するかどうかは実装前に確定する

---

## 13. エラーハンドリング

### 13.1 Git未インストール

- 表示:
  - Gitがインストールされていないこと
  - OS別インストール手順
  - 再チェックボタン
- 処理:
  - プロジェクト作成をブロックする

### 13.2 GitHub未認証

- 表示:
  - GitHubログインが必要
  - 推奨手順
- 処理:
  - 認証フローへ戻す

### 13.3 SSH接続失敗

- 表示:
  - SSH鍵の有無
  - 公開鍵登録の確認
  - 再試行
- 処理:
  - 接続成功までガイドする

### 13.4 秘密情報検出

- 表示:
  - 送信できない情報が含まれている
  - 該当箇所
  - 除外するボタン
- 処理:
  - 原則送信をブロックする

### 13.5 パッチ適用失敗

- 表示:
  - 適用できなかった理由
  - 手動適用用diff
  - 再試行
- 処理:
  - ファイルを変更しない
  - バックアップを保持する

### 13.6 AI応答失敗

- 表示:
  - AI応答に失敗
  - 先生に質問する導線
- 処理:
  - 生徒の質問作成は継続可能

---

## 14. ログ・監査

### 14.1 保存するログ

- ログイン
- クラス作成
- 生徒招待
- プロジェクト作成
- GitHub連携状態変更
- スレッド作成
- メッセージ送信
- AI利用
- パッチ提案
- パッチ適用
- パッチ失敗
- パッチ取り消し
- 権限変更

### 14.2 保存しないログ

- SSH秘密鍵の内容
- GitHubアクセストークンの平文
- `.env` の内容
- 生徒PCの絶対パスの平文
- 不必要な個人ファイル情報

---

## 15. セキュリティ

### 15.1 Electronセキュリティ

- `contextIsolation: true`
- `nodeIntegration: false`
- `sandbox: true` を検討
- preloadで公開するAPIを限定する
- 任意コマンド実行は禁止
- Git / GitHub / SSH確認コマンドはホワイトリスト化する
- 外部URLオープン時は確認を挟む

### 15.2 Supabaseセキュリティ

- RLS必須
- クラス・プロジェクト・スレッド単位のアクセス制御
- サービスロールキーをクライアントへ配布しない
- Functions内で必要な権限チェックを行う

### 15.3 GitHubセキュリティ

- PAT手入力を避ける
- トークン平文保存禁止
- 必要最小権限
- SSH秘密鍵の送信禁止
- リポジトリへの直接書き込みはMVPでは行わない

### 15.4 AIセキュリティ

- AI送信前に秘密情報チェック
- AIに送信するコード範囲を最小化
- AI出力をそのまま実行しない
- AIパッチは必ず差分確認を挟む

---

## 16. 未決定事項

1. 対応OS
   - macOSのみ
   - macOS + Windows
   - macOS + Windows + Linux

2. GitHub認証方式の最終決定
   - `gh auth login`
   - GitHub Device Flow
   - GitHub App OAuth
   - PAT最終手段

3. GitHub CLIを必須にするか
   - 必須にすると実装は簡単だがインストール手順が増える
   - 任意にすると実装分岐が増える

4. SSH接続失敗時にプロジェクト作成をブロックするか
   - ブロックする
   - 警告のみで進める
   - クラス設定で切り替える

5. AI Provider
   - OpenAI
   - その他
   - 複数対応

6. AIに送信した情報・AI出力を保存するか
   - 保存する
   - 一定期間保存
   - 保存しない
   - スレッドに残すものだけ保存

7. コード断片の保存範囲
   - GitHub参照のみ
   - 必要最小限のコード断片のみ保存
   - パッチのみ保存

8. リアルタイム通信方式
   - Supabase Realtime
   - Supabase Functions + SSE
   - 両方併用

---

## 17. 実装前に確認すべきこと

1. `gh auth login` ベースのオンボーディングUXを実機で検証する
2. GitHub Device FlowとのUX比較を行う
3. 中学1年生以上が理解できる文言でオンボーディングを設計する
4. SSH鍵作成・登録補助をどこまで自動化するか決める
5. GitHub CLIを同梱・自動インストール・手動案内のどれにするか決める
6. ElectronでローカルGit操作を行う安全なIPC設計を確定する
7. `.gitignore` 推奨生成ロジックを決める
8. 秘密情報検出ルールを決める
9. AIに送る情報の最小単位を決める
10. パッチ適用方式を決める
11. パッチ適用前バックアップ方式を決める
12. Supabase RLS設計をレビューする
13. クラス・先生・生徒の権限境界をテストする
14. コード全文を保存しない方針をどこまで厳密にするか決める

---

## 18. MVP実装タスク

### Phase 1: 基盤

- [ ] Electron + TypeScriptプロジェクトを作成する
- [ ] Supabaseプロジェクトを作成する
- [ ] DBスキーマを作成する
- [ ] Supabase RLSを設定する
- [ ] Electron IPC設計を作成する

### Phase 2: オンボーディング

- [ ] 生徒オンボーディング画面を実装する
- [ ] Gitインストール確認を実装する
- [ ] GitHub認証フローを実装する
- [ ] SSH接続確認を実装する
- [ ] `.gitignore` 生成補助を実装する

### Phase 3: チャットコア

- [ ] クラス作成・招待機能を実装する
- [ ] プロジェクト作成機能を実装する
- [ ] スレッド作成機能を実装する
- [ ] チャット機能を実装する
- [ ] 先生向け質問キューを実装する

### Phase 4: コード・環境情報

- [ ] Git差分取得を実装する
- [ ] 環境情報収集を実装する
- [ ] 送信前プレビューを実装する
- [ ] 秘密情報チェックを実装する
- [ ] コード表示・シンタックスハイライトを実装する
- [ ] 差分表示を実装する

### Phase 5: AI機能

- [ ] AI質問文生成を実装する
- [ ] AIエラー要約を実装する
- [ ] AI原因候補提示を実装する
- [ ] AIパッチ案生成を実装する

### Phase 6: パッチ提案・適用

- [ ] 先生パッチ提案UIを実装する
- [ ] パッチ確認画面を実装する
- [ ] ローカルパッチ適用を実装する
- [ ] 元に戻す機能を実装する
- [ ] パッチ適用失敗時の安全処理を実装する

### Phase 7: テスト・リリース準備

- [ ] RLSテストを実装する
- [ ] Electronセキュリティレビューを実施する
- [ ] GitHub連携の異常系テストを行う
- [ ] パッチ適用の破壊的ケースをテストする
- [ ] 秘密情報検出テストを行う
- [ ] AI失敗時のフォールバックを確認する
- [ ] macOS/WindowsでE2Eテストを実施する
- [ ] 初学者向けオンボーディング文言をレビューする

---

## 19. MVP成功条件

- 中学1年生以上の生徒が、案内に従ってGitHub/SSH接続を完了できる
- GitHub連携なしではプロジェクト作成できない
- 生徒がエラー発生時に、コード・差分・環境情報つきで質問できる
- 先生が質問キューから未対応質問を確認できる
- 先生がチャット上でパッチを提案できる
- 生徒が差分を確認して1クリックでローカル適用できる
- AIが質問文生成・エラー要約・原因候補提示・パッチ案生成を補助できる
- 先生やAIが生徒PCを直接書き換える経路が存在しない
- `.env` や秘密鍵などの送信を防止できる
- GitHub Classroomではなく、伴走型質問支援ツールとして明確に差別化できている
